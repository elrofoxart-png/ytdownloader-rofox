version: '3.8'

services:
  ytdownloader:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./downloads:/downloads
    environment:
      - FLASK_ENV=development
      - FLASK_DEBUG=0
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/"]
      interval: 30s
      timeout: 10s
      retries: 3
